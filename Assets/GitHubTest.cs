using UnityEngine;

public class GitHubTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //깃허브 용도 테스트 중입니다.
        //연동이 잘된다면 깃허브 공간에서 해당 내용이 보일 것입니다.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
